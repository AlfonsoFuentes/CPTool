
@*
<ComponentTableList ShowAddButton=false
                     TDTO="DownPaymentDTO"
                     SearchFunc=@SearchFunc
                     T="DownPayment" TableName="Downpayments"
                     OnShowDialogOverrided=ToolDialogService.ShowDownpaymentDialog>
    <ContextTh>
        <MudTh>PO #</MudTh>
        <MudTh>MWO</MudTh>
        <MudTh>Supplier</MudTh>
        <MudTh>CBS Request No</MudTh>

      
        <MudTh>Status</MudTh>
        <MudTh>PO Value</MudTh>
        <MudTh>Percentage</MudTh>
        <MudTh>DP Value</MudTh>
        <MudTh>DP Due date</MudTh>
        <MudTh>DP real date</MudTh>


    </ContextTh>
    <ContextTd>
        <MudTd DataLabel="PONumber">@context.PurchaseOrderDTO.PONumber</MudTd>
        <MudTd DataLabel="MWOName">@context.MWOName</MudTd>
        <MudTd DataLabel="Supplier">@context.PurchaseOrderDTO.SupplierDTO.Name</MudTd>
        <MudTd DataLabel="CBSRequesNo">@context.CBSRequesNo</MudTd>


        <MudTd DataLabel="DownpaymentStatus">@context.DownpaymentStatus.ToString()</MudTd>
        <MudTd DataLabel="PrizeCurrency">@($"${context.PurchaseOrderDTO.Currency} {context.PurchaseOrderDTO.PrizeCurrency}")</MudTd>
            <MudTd DataLabel="Percentage">@context.Percentage</MudTd>
            <MudTd DataLabel="DownPaymentAmount">@($"${context.PurchaseOrderDTO.Currency} {context.DownPaymentAmount}")</MudTd>
            <MudTd DataLabel="DownPaymentDueDate">@context.DownPaymentDueDate?.ToString("d",_en)</MudTd>
            <MudTd DataLabel="RealDate">@context.RealDate?.ToString("d",_en)</MudTd>


        </ContextTd>

    </ComponentTableList>*@

@code {

    public CultureInfo _en = CultureInfo.GetCultureInfo("en-US");
}
