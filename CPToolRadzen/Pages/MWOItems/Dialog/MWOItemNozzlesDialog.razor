@using CPToolRadzen.Pages.Nozzle.List
<NozzleList OnAdd=@AddNozzle ElementDetails="@Nozzles" TableName="@TableName" SaveDialog=@(Model.Id!=0)></NozzleList>
@if(debug)
{
    
}
@code {
    bool debug = true;
}
