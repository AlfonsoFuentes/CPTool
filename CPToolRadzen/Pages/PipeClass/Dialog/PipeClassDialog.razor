@using CPTool.Application.Features.PipeClassFeatures.CreateEdit;

@inherits DialogTemplate<EditPipeClass>
@if (debug)
{

}

<DialogTemplate Model="@Model">
    <ChildContent>
        <TextBoxTemplate Label="Name" Required=true @bind-Value=@Model.Name StringsToCompare=@(FilteredList.Select(x => x.Name).ToList())>

        </TextBoxTemplate>
       
    </ChildContent>

</DialogTemplate>
@code {

    bool debug = true;
   
}
